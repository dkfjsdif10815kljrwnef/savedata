import 'package:flutter/material.dart';

class BindingPageBinding extends StatelessWidget {
  const BindingPageBinding({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
